{
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "concurrently": "^9.0.1",
    "gulp": "^5.0.0",
    "gulp-replace": "^1.1.4",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.3",
    "express": "^4.21.0",
    "firebase": "^10.12.5",
    "jwt-decode": "^4.0.0"
  },
  "scripts": {
    "start": "concurrently \"firebase serve\" \"browser-sync start --proxy localhost:5000 --files 'public/**/*'\""
  }
}
